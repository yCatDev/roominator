@page "/apppage"
@inject NavigationManager NavManager
@code {

    private void StartupMethod()
    {
        Program.mobileHelper.SetEntry(Entry.APK);
        if (NavManager.Uri.Contains("localhost"))
        {
            NavManager.NavigateTo("https://localhost:5000/Identity/Pages/Account/Artemlogin");
        }
        else
        {
            NavManager.NavigateTo("https://roominator-nure.herokuapp.com/Identity/Pages/Account/Artemlogin");
        }
    }

    private Task StartupTask()
    {
        return Task.CompletedTask;
    }

    protected override async Task OnInitializedAsync()
    {
        StartupMethod();
        await StartupTask();
    }

}
